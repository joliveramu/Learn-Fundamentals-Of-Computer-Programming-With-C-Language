#include<stdio.h>

int main()
{
	int n1, n2, sum;
	printf("Enter 1st number:");
	scanf("%d", &n1);
	printf("Enter 1st number:");
	scanf("%d", &n2);
	sum = n1 + n2;
	printf("The sum of the numbers you entered is: %d", sum);
	return 0;
}